server:
  port: 8081


  spring:
    application:
      name: department-server

  spring:
    datasource:
      url: jdbc:mysql://mysql:3306/db
      username: admin
      password: admin
    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true

  eureka:
      client:
          service-url:
            defaultZone: http://localhost:8761/eureka
